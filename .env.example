# ===================================
# Backend Configuration
# ===================================
BACKEND_PORT=8000
BACKEND_HOST=0.0.0.0

# ===================================
# Frontend Configuration
# ===================================
FRONTEND_PORT=3030
FRONTEND_HOST=0.0.0.0

# Frontend API URL (untuk build frontend)
# Production: ganti dengan URL backend yang sebenarnya
VITE_API_URL=http://localhost:8000

# ===================================
# CORS Configuration
# ===================================
# Comma-separated list of allowed origins
# Production: tambahkan domain production Anda
CORS_ORIGINS=http://localhost:3030,http://localhost:5173

# ===================================
# Rust Backend Configuration
# ===================================
# Log level: trace, debug, info, warn, error
RUST_LOG=info

# Rust Dockerfile selection (auto-detected by deploy.sh)
# Dockerfile = Build from source (slower, may fail with 307KB binary issue)
# Dockerfile.prebuilt = Use pre-built binary (faster, reliable)
# RUST_DOCKERFILE=Dockerfile

# ===================================
# Docker Configuration
# ===================================
# Container names prefix (optional)
COMPOSE_PROJECT_NAME=konversi-data

# ===================================
# Production Settings
# ===================================
# Environment: development, production
NODE_ENV=production

# Python workers (untuk FastAPI)
BACKEND_WORKERS=4
